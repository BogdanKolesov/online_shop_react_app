import styled from 'styled-components';
import { calcFontSize } from "../../../functions";

const StyledLink = styled.a`
    font-family: 'Roboto Medium';
    font-size: 18px;
    color: #000;
    
`
export default StyledLink