import React from 'react';
import { Button, Container } from '../../atoms'

const Home = () => {
    return (
        <Container type='full'>
            <Button text='Hello!' font='bold' />
        </Container>
    );
}

export default Home;
